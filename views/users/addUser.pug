extends adminBase.pug

block content
    .container
        h3 Unregistered Users
        form(method="POST")
            table(class='table', id='Unregistered_User_Table')
                thead
                    tr
                        th Name
                        th ID
                        th Type
                tbody(id='Unregistered_User_Table_body')
                    each user in users
                        tr
                            td= user.profileName
                            td= user.profileID
                            td 
                                a(id="selectUser" href="/users/admin/editUser") Edit
                            
            if !users.length
                p No Unregistered users found.

            div    
                input(type="submit" value="submit") 
            // replace these br with CSS whitespace later    
        br 
        br    
        h3 Manually create user
        table(class='table')
            tbody  
                tr
                    td Full Name
                    td
                        input(type="text" name='fullName')
                tr
                    td  Email
                    td
                        input(type="text" name='fullName')
        // in retro

            